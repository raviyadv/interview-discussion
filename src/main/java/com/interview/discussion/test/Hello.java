/*
package com.interview.discussion.test;

import java.util.HashMap;
import java.util.Map;
import java.util.stream.Collectors;

public class Hello {
    public static void main(String[] args) {
        Map<String,Integer> map = new HashMap<>();
        map.put("ravi", 100);
        map.put("prithivi", 200);
        map.put("pavan", 300);
     //   map.keySet().stream()
          //      .filter(name-> !(name.getBytes()[1]=='p');
    }
    class Emp{
        int id;
        String name;
        @Override
        public int hashCode() {
            return id;
        }

        @Override
        public boolean equals(Object obj) {
            if (obj==null)
                return false;
         //   if (obj instanceof )
      //      Emp emp = (Emp) obj;
          //  if(this.id==((Emp) obj).id && this.name == ((Emp) obj).name) return true;
}
*/
